{% extends 'base.html.twig' %}

{% block title %}CmsActualites{% endblock %}

{% block body %}
    <h1>CmsActualites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cms_actualite.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ cms_actualite.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cms_actualite.description }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ cms_actualite.content }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ cms_actualite.image }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ cms_actualite.createdAt ? cms_actualite.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_news_index') }}">back to list</a>

    <a href="{{ path('app_backoffice_news_edit', {'id': cms_actualite.id}) }}">edit</a>

    {{ include('backoffice/news/_delete_form.html.twig') }}
{% endblock %}
