<form method="post" action="{{ path('app_backoffice_news_delete', {'id': cms_actualite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cms_actualite.id) }}">
    <button class="btn">Delete</button>
</form>
